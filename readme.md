### Install libraries
`yarn install`

### Start application

`yarn start`
 

You can open iOS, Android, or web from here, or run them directly with the commands below.

`yarn android` 

`yarn ios`

`yarn web`
